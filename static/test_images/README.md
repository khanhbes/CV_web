Đặt ảnh test vào thư mục này để dùng trong trang inference.

Ví dụ:
- test_image.jpg
- sample_traffic.png

Sau khi đặt ảnh ở đây, bạn có thể dùng chức năng upload trong trang inference hoặc chỉnh template để tải trực tiếp file này.